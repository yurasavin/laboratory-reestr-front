version: '3.7'

networks:
  resources:
    external: true

services:
  front:
    image: iriskin/laboratory-reestr-front:latest
    ports:
      - "80"
    networks:
      - resources
    labels:
      - "traefik.enable=true"

      - "traefik.http.routers.laboratory-django-front.rule=Host(`laboratory.kvd-rostov.ru`)"
      - "traefik.http.routers.laboratory-django-front.entrypoints=https"
      - "traefik.http.routers.laboratory-django-front.tls=true"
    restart: always
